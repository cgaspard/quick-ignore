{
  "name": "quick-ignore",
  "displayName": "Quick Ignore",
  "description": "Right-click any file or folder in the Explorer to add it to .gitignore — locally or globally.",
  "version": "0.1.1",
  "publisher": "cgaspard",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cgaspard/quick-ignore.git"
  },
  "bugs": {
    "url": "https://github.com/cgaspard/quick-ignore/issues"
  },
  "homepage": "https://github.com/cgaspard/quick-ignore#readme",
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "gitignore",
    "git",
    "ignore",
    "context menu"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "quickIgnore.addToLocalGitignore",
        "title": "Quick Ignore: Add to local .gitignore"
      },
      {
        "command": "quickIgnore.addToGlobalGitignore",
        "title": "Quick Ignore: Add to global .gitignore"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceScheme == file",
          "group": "2_gitignore",
          "command": "quickIgnore.addToLocalGitignore"
        },
        {
          "when": "resourceScheme == file",
          "group": "2_gitignore",
          "command": "quickIgnore.addToGlobalGitignore"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "node esbuild.js --watch",
    "package": "npm run check-types && node esbuild.js --production",
    "package:vsix": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.104.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
