{
  "name": "gitignore-helper",
  "displayName": "Gitignore Helper",
  "description": "Right-click any file or folder in the Explorer to add it to .gitignore — locally or globally. By cgaspard.",
  "version": "0.1.0",
  "publisher": "cgaspard",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cgaspard/gitignore-helper.git"
  },
  "bugs": {
    "url": "https://github.com/cgaspard/gitignore-helper/issues"
  },
  "homepage": "https://github.com/cgaspard/gitignore-helper#readme",
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "gitignore",
    "git",
    "ignore",
    "context menu"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gitignoreHelper.addToLocalGitignore",
        "title": "Gitignore Helper: Add to local .gitignore"
      },
      {
        "command": "gitignoreHelper.addToGlobalGitignore",
        "title": "Gitignore Helper: Add to global .gitignore"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceScheme == file",
          "group": "2_gitignore",
          "command": "gitignoreHelper.addToLocalGitignore"
        },
        {
          "when": "resourceScheme == file",
          "group": "2_gitignore",
          "command": "gitignoreHelper.addToGlobalGitignore"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "node esbuild.js --watch",
    "package": "npm run check-types && node esbuild.js --production",
    "package:vsix": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.104.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
